const botao = document.getElementById('darkMode');

// Adiciona um ouvinte de eventos ao botão
botao.addEventListener('click', () => {
  // Alterna a classe 'clicado'
  botao.classList.toggle('clicado');
  
  // Altera o texto com base no estado atual

});
// esquema de troca de cor do botão, quando pressionado.